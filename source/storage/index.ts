// login/index
import StorageReducer from './reducers';
import * as StorageTypes from './types';
import { SaveByKey, GetByKey, RemoveKey } from './actions';

export { StorageReducer, StorageTypes, SaveByKey, GetByKey, RemoveKey  };
